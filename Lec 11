import numpy as np
from scipy.interpolate import CubicSpline
x = np.array ([0, 1, 2])
y = np.array ([1, 2, 4]) 
cs = CubicSpline (x, y) 
x0 = 1.5
print (cs(x0), ' is the answer by library function.')
print (2**x0, ' is the exact answer.')
